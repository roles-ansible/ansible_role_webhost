---
default_webroot_prefix: '/srv/www/'

default_webroot_user: 'www-data'
default_webroot_group: 'www-data'
default_webroot_mode: 'u=rwx,g=rx,o='

# perform simple version check for this role? (true is recomended)
submodules_versioncheck: false
